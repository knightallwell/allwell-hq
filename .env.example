# Phase 1 (current) has no backend — nothing below is required to run `npm run dev`.
# Update src/lib/site.ts with the real production URL once deployed.

NEXT_PUBLIC_SITE_URL="https://allwellgodwin.com"

# --- Reserved for later phases (not yet used by any code) ---
# DATABASE_URL=
# NEXTAUTH_SECRET=
# NEXTAUTH_URL=
# CLOUDINARY_URL=
# RESEND_API_KEY=
